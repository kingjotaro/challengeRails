<p style="color: green"><%= notice %></p>

<%= render @user %>

<div>
  <%= link_to "Edit this user", edit_user_path(@user), class: "button action-button" %>
  <%= link_to "Back to users", users_path, class: "button action-button" %>
</div>
